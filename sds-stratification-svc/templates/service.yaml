apiVersion: v1
kind: Service
metadata:
  name: {{ include "sds-stratification-svc.fullname" . }}
  labels:
    {{- include "sds-stratification-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sds-stratification-svc.selectorLabels" . | nindent 4 }}
