apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sds-stratification-svc.fullname" . }}-test-connection"
  labels:
    {{- include "sds-stratification-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sds-stratification-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
